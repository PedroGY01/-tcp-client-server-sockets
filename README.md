# -tcp-client-server-sockets
A simple client-server application for transferring files over TCP sockets. The client requests a file by name, and the server reads the requested file, sends its contents as bytes, and the client reconstructs and saves the file locally.
